# ── REQUIRED ─────────────────────────────────────────────
OPENROUTER_API_KEY=sk-or-v1-xxxx        # openrouter.ai → Keys
OPENROUTER_MODEL=model_name
APIFY_API_TOKEN=apify_api_xxxx          # apify.com → Settings → Integrations

# ── OPTIONAL (placeholders created if missing) ───────────
ELEVENLABS_API_KEY=sk_xxxx              # elevenlabs.io → Profile → API Keys
ELEVENLABS_VOICE_ID=21m00Tcm4TlvDq8ikWAM
HF_API_TOKEN=hf_xxxx                    # huggingface.co → Settings → Tokens
GDRIVE_FILE_ID=your-doc-id-from-url     # from your Google Drive doc URL

# ── APP SETTINGS (leave as-is) ───────────────────────────
CWT_NICHE=trading signals, stock market, financial freedom
CWT_WEBSITE=crowdwisdomtrading.com
AD_DURATION_SECONDS=60
LOG_LEVEL=INFO